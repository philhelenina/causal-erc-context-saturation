[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "senticcrystal"
version = "0.1.0"
description = "Code for causal emotion recognition in conversation experiments"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
